# CleanSpeechEdit

Небольшое GUI на CustomTkinter: выбирает XML и аудио, три режима очистки (Default/Soft/Aggressive) и демо-кнопка генерации.

## Запуск
```
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
python clean_speech_edit.py
```
